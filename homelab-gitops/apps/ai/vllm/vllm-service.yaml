apiVersion: v1
kind: Service
metadata:
  name: vllm
  namespace: ai
  annotations:
    lbipam.cilium.io/ips: 192.168.2.205
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: vllm
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
