apiVersion: v1
kind: Service
metadata:
  name: llama-gemma4
  namespace: ai
  labels:
    app.kubernetes.io/name: llama-gemma4
    app.kubernetes.io/part-of: prometheus-ai
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: llama-gemma4
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
