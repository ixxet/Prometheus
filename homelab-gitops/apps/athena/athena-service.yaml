apiVersion: v1
kind: Service
metadata:
  name: athena
  namespace: athena
  labels:
    app.kubernetes.io/name: athena
    app.kubernetes.io/part-of: ashton
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: athena
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
