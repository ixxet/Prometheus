apiVersion: apps/v1
kind: Deployment
metadata:
  name: athena
  namespace: athena
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: athena
  template:
    metadata:
      labels:
        app.kubernetes.io/name: athena
        app.kubernetes.io/part-of: ashton
    spec:
      enableServiceLinks: false
      serviceAccountName: athena
      containers:
        - name: athena
          image: ghcr.io/ixxet/athena:v0.4.1@sha256:87685d3ad4e86bd9593ad39326c7ecb1210b631e6056f3c54f618322b5fddb6f
          imagePullPolicy: IfNotPresent
          args:
            - serve
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: ATHENA_HTTP_ADDR
              value: ":8080"
            - name: ATHENA_NATS_URL
              value: "nats://nats.agents.svc.cluster.local:4222"
            - name: ATHENA_IDENTIFIED_PUBLISH_INTERVAL
              value: "24h"
            - name: ATHENA_EDGE_OCCUPANCY_PROJECTION
              value: "true"
            - name: ATHENA_EDGE_HASH_SALT
              valueFrom:
                secretKeyRef:
                  name: athena-edge-runtime
                  key: ATHENA_EDGE_HASH_SALT
            - name: ATHENA_EDGE_TOKENS
              valueFrom:
                secretKeyRef:
                  name: athena-edge-runtime
                  key: ATHENA_EDGE_TOKENS
          readinessProbe:
            httpGet:
              path: /api/v1/health
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/v1/health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              memory: 128Mi
