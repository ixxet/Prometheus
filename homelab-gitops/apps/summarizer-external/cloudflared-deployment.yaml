apiVersion: apps/v1
kind: Deployment
metadata:
  name: summarizer-tunnel
  namespace: summarizer
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: summarizer-tunnel
  template:
    metadata:
      labels:
        app.kubernetes.io/name: summarizer-tunnel
    spec:
      containers:
        - name: cloudflared
          image: cloudflare/cloudflared:2026.3.0
          args:
            - tunnel
            - --no-autoupdate
            - --metrics
            - 0.0.0.0:2000
            - --url
            - http://summarizer-proxy.summarizer.svc.cluster.local:8080
          ports:
            - name: metrics
              containerPort: 2000
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              memory: 128Mi
