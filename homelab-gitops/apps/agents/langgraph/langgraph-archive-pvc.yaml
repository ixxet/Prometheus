apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: langgraph-archive
  namespace: agents
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: ""
  volumeName: langgraph-archive-pv
