# Placeholder values only. Replace and encrypt with SOPS before unsuspending
# the apps layer. LangGraph standalone deployments also require Redis and
# LangSmith/LangGraph credentials at runtime.
apiVersion: v1
kind: Secret
metadata:
  name: langgraph-runtime
  namespace: agents
type: Opaque
stringData:
  DATABASE_URI: postgresql://langgraph:change-me-before-enable@postgres.agents.svc.cluster.local:5432/langgraph
  REDIS_URI: redis://redis.agents.svc.cluster.local:6379/0
  LANGSMITH_API_KEY: replace-before-enable
  LANGGRAPH_CLOUD_LICENSE_KEY: replace-before-enable
  OPENAI_API_KEY: not-used-for-local-vllm
