apiVersion: v1
kind: Service
metadata:
  name: langgraph
  namespace: agents
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: langgraph
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
