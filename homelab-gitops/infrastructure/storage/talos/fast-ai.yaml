# DANGEROUS: Talos machine config, not a Kubernetes manifest.
# Parked until a dedicated second SSD or NVMe exists for a fast AI tier.
apiVersion: v1alpha1
kind: UserVolumeConfig
name: fast-ai
provisioning:
  diskSelector:
    match: disk.transport == 'nvme' && disk.serial == 'S6S2NS0TC17843N'
  minSize: 500GiB
  maxSize: 1500GiB
filesystem:
  type: xfs
