# Talos machine config, not a Kubernetes manifest.
# This is the first-wave temporary storage path: a directory-backed user volume
# on the system SSD EPHEMERAL partition. It avoids repartitioning any other disk.
apiVersion: v1alpha1
kind: UserVolumeConfig
name: local-path-provisioner
volumeType: directory
