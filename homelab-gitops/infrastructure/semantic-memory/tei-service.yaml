apiVersion: v1
kind: Service
metadata:
  name: tei-embeddings
  namespace: semantic-memory
spec:
  selector:
    app.kubernetes.io/name: tei-embeddings
  ports:
    - name: http
      port: 80
      targetPort: http
