apiVersion: v1
kind: Service
metadata:
  name: qdrant
  namespace: semantic-memory
spec:
  selector:
    app.kubernetes.io/name: qdrant
  ports:
    - name: http
      port: 6333
      targetPort: http
