apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: apollo
  namespace: observability
spec:
  namespaceSelector:
    matchNames:
      - agents
  selector:
    matchLabels:
      app.kubernetes.io/name: apollo
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
